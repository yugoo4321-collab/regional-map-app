# Design System

## 構成

- `assets/app.css`：アプリ共通のCSS
- `ui_theme.py`：CSSの読込
- `app.py`：データ処理と画面構成
- `UI_COPY_GUIDE.md`：画面文言の基準

## 方針

- 本文は濃い青灰色、背景は薄い青灰色、紙面は白
- 差し色は青を基本に、茶と青紫を限定的に使う
- 角丸と影は情報の階層を作る範囲だけ
- 見出しは短く、本文は常体、ボタンは動作名
- 色だけで状態を伝えず、文字、線、太さを併用する

## アクセシビリティ

- キーボードフォーカスを表示
- タブを横スクロール可能にする
- `prefers-reduced-motion`へ対応
- 高コントラストモードへ対応
- 狭い画面では左右の余白を縮小
